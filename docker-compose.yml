version: '3.8'

services:
  frontend:
    build: ./frontend
    container_name: plant-frontend
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
